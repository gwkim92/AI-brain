import { OperatorShell } from "@/components/layout/product/ProductShell";

export default function IntelligenceLayout({ children }: { children: React.ReactNode }) {
  return <OperatorShell>{children}</OperatorShell>;
}
